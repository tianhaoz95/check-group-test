name: A Ton of Checks
on:
  pull_request:
    branches:
      - "master"
    paths:
      - "a-ton-of-checks.md"
  push:
    branches:
      - "master"
    paths:
      - "a-ton-of-checks.md"
strategy:
  matrix:
    run_id: [1, 2, 3]
jobs:
  create-a-ton-of-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Run ${{ matrix.run_id }}
        run: echo "fake check $RUN_ID"
        env:
          RUN_ID: ${{ matrix.run_id }}
